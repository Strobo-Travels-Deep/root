# Hasse et al. — Breakwater Dossier

**Dossier v0.7 · Breakwater Layer · Rocket Science Demonstrator**

Claim Analysis Ledger and interactive simulation platform for:

> F. Hasse, D. Palani, R. Thomm, U. Warring, T. Schaetz,
> *Phase-stable travelling waves stroboscopically matched for super-resolved observation of trapped-ion dynamics*,
> [Phys. Rev. A **109**, 053105 (2024)](https://doi.org/10.1103/PhysRevA.109.053105) ·
> [arXiv: 2309.15580](https://arxiv.org/abs/2309.15580)

**Endorsement Marker:** Local candidate framework (no parity implied with externally validated laws).

## Structure

| Page | Description |
|------|-------------|
| `index.html` | Overview, navigation, quick summary |
| `dossier.html` | Claim Analysis Ledger (8 entries), Risk Register, Council Decisions |
| `framework.html` | Interaction Hamiltonian, measurement channels, Lock-Key assignments |
| `tutorial.html` | Doppler mechanism, analytic estimates, Work Packages A-D |
| `numerics.html` | Interactive viewer: pre-computed HDF5, simulation run JSON, user uploads |
| `simulate.html` | **In-browser quantum simulation** with decoherence and projection noise |

## Simulate page (v0.7)

Visitors can run the full stroboscopic detuning scan in their browser:

- **Parameters:** alpha, eta, omega_m, Omega, N_max, detuning range, pulse count
- **Decoherence:** Spin T1, T2, motional heating (dn/dt) via quantum trajectories
- **Projection noise:** Configurable N_rep with binomial sampling and error bars
- **Convergence diagnostics:** Fock truncation checked post-run with colour-coded banner
- **Downloads:** JSON, CSV, Python/QuTiP script, manifest — all with SHA-256 provenance hash

## Data

- `data/alpha_*/detuning_scan.h5` — Pre-computed HDF5 from adaptive-learner simulations
- `data/manifest.json` — HDF5 dataset manifest
- `data/runs/*.json` — Default simulation runs (generated by simulate.html engine)
- `data/runs/manifest.json` — Simulation run manifest

### Cross-validation note

The HDF5 datasets (`data/alpha_*/`) were generated by an adaptive-learner simulation
pipeline that integrates the stroboscopic evolution using many small time steps.
The default JSON runs (`data/runs/`) use the simulate.html engine with **22 stroboscopic
pulses** — one per motional cycle — matching the physical protocol described in the paper.

Because the two engines model the pulse interaction differently (continuous integration
vs. discrete stroboscopic kicks), their results will differ quantitatively, especially
for features sensitive to the pulse model (e.g. sideband structure at large α). Both
are valid within their respective approximations. Users should compare results with
awareness of this distinction.

## Provenance

Every simulation result carries a SHA-256 hash covering: code version, repository URL,
all parameters (including decoherence and noise settings), timestamp, output array
fingerprints, and Fock convergence diagnostics. This hash enables traceability if results
are merged into the open-research-platform database.

## Architecture notes

- `simulate.html` is self-contained (inline CSS/JS) for portability. For a production
  merge into the Harbour site, the CSS should be extracted to `style.css`.
- The numerics viewer (`js/numerics-viewer.js`) handles three data sources: HDF5 (via
  jsfive), JSON runs, and user uploads.
- Default runs in `data/runs/` use **22 stroboscopic pulses** (one per motional cycle)
  at α = 0, 1, 3, 5. Note: α = 5 with N_max = 40 shows 1.1% Fock leakage — this is
  genuine and the convergence banner correctly flags it.

## v0.7 changelog

- Fixed numerics.html settings panel: "Pulses in train" corrected from 326 to 22.
- Fixed manifest.json: contrast_z values recomputed from JSON run data (≈ 0.56 for all α).
- Added cross-validation caveat to README distinguishing HDF5 and JSON pulse models.
- Tutorial: σ_z contrast values flagged as HDF5-derived, not reproducible from JSON runs.
- simulate.html: decoherence collapse probability saturation check added.
- simulate.html: Fock convergence now tracked across all trajectories (not just last).
- export_hdf5.py: n_pulses default corrected from 326 to 22.

## Licence

Part of the Open Science Harbour. MIT.
